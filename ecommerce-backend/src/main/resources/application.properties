# Server Port (Frontend runs on 5173, Backend will run on 8080)
server.port=8080

# MySQL Database Connection Details
spring.datasource.url=jdbc:mysql://localhost:3306/luxecart?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Jamshedpur@1

# Hibernate (JPA) Settings
# 'update' tells Spring to automatically create or update tables based on our Java classes!
spring.jpa.hibernate.ddl-auto=update

# Show the actual SQL queries in the console (great for debugging)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true